# 项目问题记录

## 2026-08-05：业主ID不能为空
- **接口**：POST /api/owner/orders?ownerId=1
- **报错**：业主ID不能为空
- **原因**：`RepairOrder` 类中 `ownerId` 字段有 `@NotNull` 校验，但请求体未传该字段
- **解决**：请求体中加入 `"ownerId": 1`

## 2026-08-05：Redis序列化报错
- **报错**：`ClassNotFoundException`
- **原因**：`RedisTemplate` 默认使用JDK序列化，对象未实现 `Serializable`
-**解决**：改用 `StringRedisTemplate` + Jackson 手动JSON序列化



这个问题出现在删除报修单功能上，根本原因是 Redis 缓存了过期的数据，和数据库不一致了。
问题的根因：Redis 缓存了旧数据，getOrdersByOwnerId 先从缓存取，缓存里有 6 条不存在的订单。点删除时 selectById 去数据库查，当然找不到。

修复内容：

deleteOrder 方法加了缓存清除 — 删除后清掉该业主的列表缓存
已清空 Redis 旧缓存 — 刷新页面就能看到真实数据